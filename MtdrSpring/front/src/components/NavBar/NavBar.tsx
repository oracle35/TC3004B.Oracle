import { Typography } from "@mui/material";

/**
 * TODO: Add Navbar Menu to improve design and consistency.
 */

const NavBar = () => {
  return (
    <div>
      <Typography>This is an example test.</Typography>
    </div>
  );
};

export default NavBar;

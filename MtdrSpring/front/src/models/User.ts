/**
 * User model
 * This model represents a user in the system.
 * It contains information about the user such as their ID, name, and email.
 */

export interface User {
  id_User: number;
  id_Telegram: string;
  name: string;
  position: string;
} 